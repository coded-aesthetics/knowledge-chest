/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { HighlightTasksDirective } from './highlight-tasks.directive';

describe('HighlightTasksDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightTasksDirective();
    expect(directive).toBeTruthy();
  });
});
