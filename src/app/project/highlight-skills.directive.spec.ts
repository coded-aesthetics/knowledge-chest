/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { HighlightSkillsDirective } from './highlight-skills.directive';

describe('HighlightSkillsDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightSkillsDirective();
    expect(directive).toBeTruthy();
  });
});
